export interface Feature {
  id: string;
  index: string;
  title: string;
  image: string;
  shortDescription: string;
}

export interface HowItWork {
  id: string;
  index: string;
  title: string;
  description: string;
}
